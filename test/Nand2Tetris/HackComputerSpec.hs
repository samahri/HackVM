module Nand2Tetris.HackComputerSpec (
    spec
) where

import Test.Hspec

spec :: Spec
spec = do
    specify "cpu" pending
    specify "rom32K" pending
    specify "screen" pending
    specify "keyboard" pending
    specify "ram64K" pending
    specify "hackComputer" pending